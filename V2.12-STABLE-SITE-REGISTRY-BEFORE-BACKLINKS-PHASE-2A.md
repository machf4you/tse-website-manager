# RESTORE POINT: V2.12-STABLE-SITE-REGISTRY-BEFORE-BACKLINKS-PHASE-2A

**Tag**: `v2.12-stable-site-registry-before-backlinks-phase-2a`  
**Date**: September 7, 2026  
**Status**: STABLE & VERIFIED (Production VPS & DEV Database)  
**Application**: TSE Site Registry (`https://site-registry.thesearchequation.co.uk/`)

---

## 1. CURRENT APPLICATION & VISUAL DESIGN STATE

* **Visual Architecture**: Full dark TSE Site Registry design system (`#070b13` background canvas, `#090e1a` surface panels, `border-white/[0.08]`, crisp typography, emerald/blue/amber status accents).
* **Permanent Refresh Beacon**: Header includes a high-visibility, pulsing emerald `[ 🔄 Refresh ]` button in `AppHeader.tsx` and an automated build-polling banner (`UpdateRefreshBanner.tsx`).
* **Dashboard (`/`)**: Clean 5-tile overview (`Total Domains`, `Active`, `Hosted`, `For Sale`, `Expiring Soon`), Attention Required queue, Portfolio distribution cards (TSE, Chili, Other, Unassigned), and Platform overview. All secondary duplicate commentary text removed.
* **Domains Page (`/domains` & `/site-registry`)**:
  * Redesigned Master Domain Registry list with standard clean TSE UI font (monospace styling removed).
  * Domain name font size increased by 1px (`text-[15px]`, `font-semibold`).
  * Duplicated secondary URLs removed (shows only canonical domain name).
  * Expanded usable table width (`max-w-[1600px]`, container positioned closer to left sidebar).
  * Primary Table Columns: `DOMAIN`, `PORTFOLIO`, `STATUS`, `PLATFORM`, `SETUP`, `ACTION`.
  * Owner completely removed from interface display (retained underneath in database for compatibility).
  * Prominent `+ Add Domain` modal dialog inserting directly into `public.domains` with automatic canonical normalization.
  * Setup Status indicator dynamically computed from existing relations (`Complete`, `1 item missing`, `2 items missing`, `Needs Setup`) with parked/for-sale status awareness.
* **Domain Control Page (`/domains/:id` & `/site-registry/:id`)**:
  * Dedicated master control center for individual domain entities.
  * Permanent Master Domain UUID displayed with 1-click clipboard copy.
  * Modular tabs prepared for subsequent development: `Overview`, `Registrar`, `Hosting`, `Website`, `Monitoring`, `SEO`, `Backlinks`, `Integrations`.

---

## 2. DATABASE & ENTITY AUTHORITY

* **Master Authority**: `public.domains` in Supabase is the single authoritative Master Domain entity.
* **Immutable UUID Architecture**: All domain operations revolve around permanent Master Domain UUIDs (`public.domains.id`).
* **Active Domain Count**: Exactly **126 master domain records** in `public.domains`.
* **DEV Database**: `aws-1-eu-west-1.pooler.supabase.com:5432` (Project: `cbdfjdxqhqajzjblysqd`). Row Level Security (RLS) configured to allow read/write management for site registry.
* **LIVE Database**: Supabase project `fqqghgqduldfmvbtnmud` remains **100% untouched**.

---

## 3. BACKLINKS SUBSYSTEM STATUS

* **Existing Backlink Data Intact**:
  * `backlink_providers`: **6 records** (*1-Zoom*, *Goodmenproject*, *icopify*, *Talk Business*, *Boost*, *Mac*)
  * `backlink_provider_sites`: **1,372 records** (Publisher / inventory directory)
  * `backlink_targets`: **530 records** (Landing pages & target phrases across 37 client domains)
  * `backlinks`: **740 records** (Published post / article records)
  * `backlink_items`: **1,204 records** (Individual outgoing backlinks embedded in those posts)
* **Backlinks Phase 1 Audit**: Completed and approved.
* **Backlinks Phase 2 Architecture**: Comprehensive workflow designed in principle (Master Sources, Campaign Batch Builder, 2-domain pairing rotation, Source deduplication, 1-click ingestion, Automated HTML/rel verification, Indexation queue, 14-day health check, Exception Action Center).
* **Anchor Pool Architecture**: Designed in principle (4-tier natural variations per target page, usage history tracking, automatic fresh anchor generation).
* **Implementation Status**: **ZERO** Phase 2 or Phase 2A backlink code, database schemas, or UI modifications have been started or executed.

---

## 4. WEBSITE MANAGER INTEGRATION STATUS

* **Pre-Check Audit**: Completed.
* **Storage Reality**: Website Manager runs as an independent PM2 service (`website-manager-api`) using a local SQLite database at `/opt/tse-apps/website-manager/server/website_manager.db` (4 website rows, 2 configured landing pages).
* **Connection Status**: Site Registry and Website Manager are **NOT yet connected** by Master Domain UUID.
* **Integration Plan**: Phase 2A will adopt the immutable `public.domains.id` as the `site_id` in Website Manager SQLite to prevent duplicate master domain authorities.

---

## 5. INFRASTRUCTURE & DEPLOYMENT CONFIGURATION

* **Production VPS**: `77.245.157.66` (Port `22667`, User `root`)
* **Live Public URL**: `https://site-registry.thesearchequation.co.uk/`
* **Reverse Proxy**: Nginx 1.24.0 with Forward Authentication via `tse-auth-service` (Port 3004) and no-cache headers for instant frontend updates.
* **Process Manager**: PM2 managing:
  * ID 0: `website-manager-api` (Port 3001)
  * ID 1: `page-auditor-api`
  * ID 2: `tse-leadgen-deployer`
  * ID 3: `site-registry-api` (Port 3006, `/opt/tse-apps/site-registry`)
  * ID 4: `tse-auth-service` (Port 3004)
  * ID 5: `lead-gen-api`

---

## 6. GIT REPOSITORY

* **Repository**: `https://github.com/machf4you/tse-site-registry.git`
* **Branch**: `main`
* **Commit Base**: `a28dce1` (Clean working tree)
