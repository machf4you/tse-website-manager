# RESTORE POINT: V2.14-STABLE-BACKLINKS-PHASE-2A-FOUNDATIONS

**Restore Point Name**: `V2.14-STABLE-BACKLINKS-PHASE-2A-FOUNDATIONS`  
**Git Tag**: `v2.14-stable-backlinks-phase-2a-foundations`  
**Date**: 07-09-2026 18:18  
**Scope**: Site Registry & Backlinks Subsystem Phase 2A Foundations

---

## 1. EXECUTIVE SUMMARY

This restore point captures the verified completion of **Backlinks Phase 2A Foundations** while preserving the full **V2.13 shared-domain foundation** across TSE Site Registry and TSE Website Manager.

All existing backlink datasets remain completely intact, all 530 backlink target landing pages and 1,204 backlink items are linked to Master Domain UUIDs, the Master Source Database (1,372 sources) has operational usage and 2-client deduplication checks, and the target-page Anchor Pool foundation has been created and populated with authoritative historical usage.

---

## 2. CORE ARCHITECTURAL FOUNDATION

* **Master Domain Identity Authority**:
  * `public.domains.id` in Site Registry (DEV Supabase `cbdfjdxqhqajzjblysqd`) is the permanent Master Domain UUID authority.
  * Exactly **126 Master Domain records** maintained unchanged.
  * Direct browser/anon mutations to `public.domains` are blocked (RLS SELECT-only).
  * Secure backend mutation path active via Express backend (`POST /api/domains`, `DELETE /api/domains/:id`).

* **Website Manager Integration (V2.13 Preserved)**:
  * Website Manager retains internal `websites.id` primary keys and all 180 existing `page_configurations`.
  * `websites.domain_id` permanently references `public.domains.id` (14/14 production sites mapped).
  * Partial unique index `idx_websites_unique_domain_id` enforces 1:1 domain binding.
  * Idempotent reconciliation mechanism creates lightweight `NEW FROM SITE REGISTRY` / `SETUP REQUIRED` shells for active/development domains.

---

## 3. BACKLINKS PHASE 2A FOUNDATIONS DELIVERED

* **Master Domain Linkage**:
  * Added `domain_id UUID REFERENCES public.domains(id)` to `public.backlink_targets` and `public.backlink_items`.
  * **530 / 530 targets linked** to Master Domain UUIDs (**0 unmatched / 0 ambiguous**).
  * **1,204 / 1,204 items linked** to Master Domain UUIDs (**0 unmatched / 0 ambiguous**).
  * Existing legacy `website_id` and `target_website_id` columns preserved intact for backwards compatibility.

* **Master Source Database (1,372 Records)**:
  * Physical table `backlink_provider_sites` preserved (no table renaming).
  * UI and architecture updated to treat it as the **Master Source Database**.
  * Exactly **1,372 publisher source records** preserved across 6 providers.
  * Backend availability & deduplication endpoint: `GET /api/backlinks/sources/availability`.
  * Deterministic 2-client deduplication dynamically excludes sources already used by either client in backlink history.

* **Anchor Pool Schema & Foundation**:
  * Created `public.backlink_anchors` table with columns: `id`, `domain_id`, `target_id`, `target_url`, `anchor_text`, `anchor_type` (`exact`, `semantic`, `commercial`, `branded`), `status` (`available`, `used`, `retired`), `times_used`, `last_used_at`, `created_at`, `updated_at`.
  * Unique constraint on `(domain_id, target_url, anchor_text)`.
  * RLS policies enabled (anon read, authenticated manage).
  * **1,637 total anchors in pool**:
    * **1,126 historically used anchors** backfilled with usage counts and last used dates from `backlink_items`.
    * **511 available target phrases** initialized from `backlink_targets`.
    * Classified by anchor type: 1,430 exact, 126 semantic, 61 branded, 20 commercial.

* **Website Manager SEO Context Bridge**:
  * Operational bridge `GET /api/bridge/domains/:domain_or_id/seo-context` delivers live landing pages, target phrases, page types, and priority directly from Website Manager page configurations.
  * No unnecessary data duplication.

* **Backlinks UI Enhancements**:
  * Exposes Master Domain association and canonical domain names.
  * Master Source Database terminology in inventory views.
  * Target Pages & Anchor Pool dual-view tab with anchor type badges, status badges, and usage metrics.

---

## 4. WHAT WAS NOT IMPLEMENTED (DEFERRED TO SUBSEQUENT PHASES)

* AI article generation
* Campaign / article batching
* Indexation queue / Google Indexing API
* Automated backlink health crawling & verification
* Ahrefs API metrics integration
* Automated scheduling & ranking integrations

---

## 5. ENVIRONMENT & SAFETY STATUS

* **DEV Supabase (`cbdfjdxqhqajzjblysqd`)**: Updated with `domain_id` columns, `backlink_anchors` table, and anchor backfill.
* **LIVE Supabase (`fqqghgqduldfmvbtnmud`)**: **100% UNTOUCHED**.
* **Website Manager SQLite (`77.245.157.66`)**: 14 websites and 180 page configurations **100% UNCHANGED**.
* **Master Domains**: 126 records **100% UNCHANGED**.
* **Existing Backlink History**: 6 providers, 1,372 provider sites, 530 targets, 740 backlinks, 1,204 items **100% PRESERVED**.
